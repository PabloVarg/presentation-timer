version: "2"
sql:
  - engine: "postgresql"
    queries: "."
    schema: "../../migrations/"
    gen:
      go:
        package: "queries"
        out: "sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
